import * as React from "react";
import { Layout } from "../Layout";

const IndexPage = () => {
  return (
    <Layout>
      <main>
        <title>Home Page</title>
        Posts
      </main>
    </Layout>
  );
};

export default IndexPage;
